# Connections (BYO / Factories)

Connection hook points — `WorkerConnections` per-role overrides and the
`PoolFactory` / `ConnFactory` / `RedisFactory` type aliases. See the
[Managed Identities & BYO Connections guide](../guides/managed-identities.md)
for the ownership model and deployment patterns.

::: taskq.connections
