# HashiCorp Vault

`VaultDynamicDbProvider` for Vault database secrets engine dynamic
credentials, backed by `hvac`. Part of the `taskq[vault]` extra. See the
[Managed Identities guide](../guides/managed-identities.md) for prerequisites
and a full worker example.

::: taskq.vault
